


export type User = {
    _id: string,
    first_name: string,
    last_name: string,
    email: string,
    is_verified: boolean,
    two_factor_enabled: boolean,
    role: string,
    createdAt: Date,
    updatedAt: Date
}

